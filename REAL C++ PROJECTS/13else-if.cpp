#include<iostream>
 using namespace std;
  /*int*/main(){
         int a;
         cout<<"Enter the number"<<endl;
         cin>>a;
         if (a<0)
         {
             cout<<"The number "<<a<<" is a negetive number ";
         }
         else if(a>0)
         {
             cout<<"the number "<<a<<" is a posetive number ";
     
         }
         else
             cout<<"the number is zero";
        
         // return 0;
 }